name: Scheduled Trigger - Vinoth

on:
  schedule:
    - cron: '*/1 * * * *'

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Print Job Status Every 30 Seconds
        run: |
          echo "JOB is working - First Run"
          sleep 30
          echo "JOB is working - Second Run"
